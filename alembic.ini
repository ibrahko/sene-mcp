# Developer entry point: `uv run alembic upgrade head` or `uv run alembic revision -m "..."`.
# The database URL comes from SENE_DATABASE_URL (default: sqlite:///sene.db).
[alembic]
script_location = src/sene_mcp/db/migrations
